# 项目沟通记录

## 2026-07-15

### 一、前端精细化升级

#### A. 接入真实 ECharts 图表

**问题描述**：所有图表区域均为文字占位符，视觉效果差。

**解决方法**：
1. 安装 `echarts` 依赖
2. 封装 [BaseChart.vue](frontend/src/components/BaseChart.vue) 组件，统一处理图表初始化、resize、销毁
3. 创建 [chartTheme.ts](frontend/src/utils/chartTheme.ts)，统一配色体系和图表配置
4. 替换所有页面图表占位符：
   - Dashboard：多折线趋势图（带渐变填充）+ 环形饼图
   - Trends：面积图、情绪折线图、堆叠面积图、渐变柱状图
   - EventDetail：帖子时间趋势小折线、风险雷达图

**涉及文件**：
- `frontend/src/components/BaseChart.vue`（新建）
- `frontend/src/utils/chartTheme.ts`（新建）
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/Trends.vue`
- `frontend/src/pages/EventDetail.vue`

---

#### B. 图标系统升级

**问题描述**：页面使用大量 emoji（💬🔴⚠️📈🍩🛡️等），风格不统一，不够专业。

**解决方法**：
1. 安装 `lucide-vue-next` 矢量图标库
2. 封装 [AppIcon.vue](frontend/src/components/AppIcon.vue) 组件，统一映射图标名称与 lucide 组件
3. 路由配置中 emoji 图标改为字符串名称（`📊` → `layout-dashboard`）
4. 替换所有页面的 emoji 为矢量图标：侧栏导航、Logo、顶栏、统计卡、Monitoring、EventDetail、Reports、Assistant

**涉及文件**：
- `frontend/src/components/AppIcon.vue`（新建）
- `frontend/src/router/index.ts`
- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/Monitoring.vue`
- `frontend/src/pages/EventDetail.vue`
- `frontend/src/pages/Reports.vue`
- `frontend/src/pages/Assistant.vue`

---

#### C. 统计卡增强 + 微动效

**问题描述**：统计卡只有数字和文字，缺乏动态效果和趋势展示。

**解决方法**：
1. 创建 `lastNDays()` 日期生成函数，避免假日期
2. 添加 [useCountUp.ts](frontend/src/utils/useCountUp.ts) composable，实现数字从0增长的缓动动画（easeOutCubic）
3. 统计卡底部添加迷你 sparkline 折线图，展示近7天趋势
4. 修复趋势箭头语义 bug：安全事件减少用绿色 ↓（`trend: 'good'`）
5. 高风险预警圆点添加 `pulse-dot` 脉冲扩散动画
6. 卡片 hover 添加 `-translate-y-0.5` 浮起效果

**涉及文件**：
- `frontend/src/utils/useCountUp.ts`（新建）
- `frontend/src/utils/chartTheme.ts`
- `frontend/src/style.css`
- `frontend/src/pages/Dashboard.vue`

---

#### E+F. 事件详情时间轴 + 风险雷达

**问题描述**：事件详情页相关帖子用表格展示不够直观，风险判断缺乏可视化。

**解决方法**：
1. 在风险判断依据旁添加6维度雷达图（讨论量/增长速度/负面情绪/事件类型/影响范围/紧急程度）
2. 相关帖子表格改为竖向时间轴布局：左侧竖线 + 圆点节点 + 内容卡片，更直观呈现事件演进

**涉及文件**：
- `frontend/src/pages/EventDetail.vue`

---

### 二、问题修复

#### 问题1：圆角矩形内文字遮挡

**问题描述**：卡片使用 `rounded-2xl` + `overflow-hidden` 导致内容被裁切，风险等级徽章被遮挡。

**解决方法**：
1. 将所有圆角矩形改为直角（移除 `rounded-2xl`、`rounded-xl`、`rounded-lg`）
2. 移除 `.stat-card` 的 `overflow-hidden` 属性
3. 所有卡片、徽章、按钮、输入框统一改为直角样式

**涉及文件**：
- `frontend/src/style.css`
- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/EventDetail.vue`
- `frontend/src/pages/Settings.vue`
- `frontend/src/pages/Reports.vue`
- `frontend/src/pages/Assistant.vue`
- `frontend/src/pages/Trends.vue`

---

#### 问题2：报告中心和系统设置页面适配问题

**问题描述**：Settings 页面使用 `max-w-3xl` 限制宽度，大屏下右侧大片空白；Reports 预览区按钮在窄屏挤压。

**解决方法**：
1. Settings：移除 `max-w-3xl`，改为响应式两列布局（`xl:grid-cols-2`），议题分类改为两列排列
2. Reports：预览区头部改为 `flex-wrap`，按钮组允许换行，预览文本容器加 `overflow-x-auto`

**涉及文件**：
- `frontend/src/pages/Settings.vue`
- `frontend/src/pages/Reports.vue`

---

#### 问题3：图表假日期（6/31、6/41）

**问题描述**：趋势分析页面面积图 X 轴日期使用字符串拼接 `${6}/${28 + i}`，导致6月出现不存在的31日、41日等假日期。

**解决方法**：
1. 创建 `lastNDays(n)` 函数，使用 JavaScript `Date` 对象动态生成日期标签
2. 自动处理跨月问题，确保日期有效性
3. 替换所有硬编码日期数组

**涉及文件**：
- `frontend/src/utils/chartTheme.ts`

---

#### 问题4：趋势分析图表与实际数据不符

**问题描述**：趋势分析页面四个图表显示的数值仍是 `chartTheme.ts` 中的默认 mock 数据，未使用后端真实数据；TOP5 议题显示不合理增长率。

**根本原因**：前后端字段名不匹配，前端无法读取后端数据，图表 fallback 到默认 mock 值。

**解决方法**：
1. 后端字段对齐：`days`→`labels`，`values`→`data`，堆叠图改为 `series: [{name, data}]` 格式
2. TOP5 占比基数修复：改为使用全部安全相关帖子数作为分母，反映真实占比
3. TOP5 语义明确：从"增长率"改为"占比"，标题改为"安全议题分布 TOP5"
4. 面积图时间窗口统一：从14天改为7天
5. 清除默认 mock 数据，避免误导

**涉及文件**：
- `backend/src/main/java/com/nankai/yuqing/controller/TrendsController.java`
- `frontend/src/pages/Trends.vue`
- `frontend/src/utils/chartTheme.ts`

---

#### 问题5：前端卡片内边距（侧边距）问题

**问题描述**：所有卡片内文字过于贴近左边缘（顶格），多次调整 padding 后仍未生效。

**根本原因**：多个旧前端进程占用端口，用户看到的是旧代码缓存；`.card-pad`/`.stat-card` 的 padding 可能被其他规则覆盖。

**解决方法**：
1. 强制杀掉所有旧 node 进程（共6个），干净启动前端
2. 给 `.card`、`.card-pad`、`.stat-card` 的 padding 添加 `!important` 强制生效
3. 最终值：`padding: 32px 52px !important`（上下32px，左右52px）

**涉及文件**：
- `frontend/src/style.css`
- `frontend/src/layouts/MainLayout.vue`

---

#### 问题6：趋势分析第四个图表语义错误

**问题描述**：第四个柱状图标题为"各来源渠道对比"，副标题写"校园集市/小红书/微博/B站"，但本项目仅针对校园集市；后端实际按 `categoryName`（打听求助/二手闲置/恋爱交友）统计，语义不符。

**解决方法**：
1. 后端 `buildSourceData()` 改为按 `safetyCategory`（安全类别）统计
2. 前端标题改为"各类帖子类型分布"，副标题改为"按安全类别统计"
3. 验证：API 返回正确显示各类安全帖子数量，总数 907 条与真实数据一致

**涉及文件**：
- `backend/src/main/java/com/nankai/yuqing/controller/TrendsController.java`
- `frontend/src/pages/Trends.vue`

---

#### 问题7：前后端数据校验

**问题描述**：确认每个板块的数据分析是否基于真实的 907 条爬取数据。

**校验结果**：
- ✅ Dashboard：总量 907，安全事件 23，高风险 3，与真实数据一致
- ✅ Monitoring：分页查询基于真实数据，搜索/筛选/分页全部可用
- ✅ Trends：4个图表数据均来自后端真实统计（areaData: [0,0,0,0,350,401,156]，总量 907）
- ✅ 事件管理：基于真实帖子聚合，无随机数
- ✅ 所有统计逻辑已移除 `Math.random()`，全部基于数据库真实数据

**涉及文件**：全项目前后端接口

---

### 三、后端接口数据结构梳理

**任务描述**：梳理前端所需后端 API 接口和数据结构，供后端开发参考。

**输出**：整理了8个核心 API 接口（Dashboard、事件列表、事件详情、舆情监测、趋势分析、报告中心、系统设置、智能助手）及对应的 JSON 数据结构，并给出数据库表设计建议。

---

## 2026-07-16

### 一、项目识别与新增数据核验

#### 1. 项目功能与需求梳理

**任务描述**：识别项目现有内容，理清业务目标、主要功能、技术结构与待完善需求。

**梳理结果**：
1. 项目定位为“校园集市安全舆情分析与研判平台”
2. 业务链路为：帖子导入 → 分类与情绪分析 → 风险评分 → 事件聚合 → 人工研判 → 趋势与报告
3. 前端包含舆情总览、舆情监测、事件管理、趋势分析、智能助手、报告中心、系统设置和数据管理等模块
4. 后端采用 Spring Boot + JPA + H2，前端采用 Vue 3 + TypeScript + ECharts
5. 识别出原有规则误报、事件过度聚合、设置未持久化、任务二标准未到位等问题

---

#### 2. 新增近万条数据体检

**数据文件**：`posts(2).json`

**核验结果**：
- 数据总量：9,753 条
- 原 `posts.json`：907 条
- 新文件已完整包含原 907 条数据，不能直接叠加
- 新文件内部重复 ID：0 条
- 正文缺失：671 条，但标题和发布时间完整
- 时间范围：2026-05-26 至 2026-07-15
- 来源分类：二手闲置 5,083 条、打听求助 3,606 条、恋爱交友 1,054 条、兼职招聘 10 条

**解决方法**：
1. 后端默认读取 `posts(2).json`
2. 启动时按帖子 ID 自动增量导入，已有数据不会重复写入
3. 数据库已有旧数据时不再直接跳过初始化，而是继续检查新增记录
4. 分析规则版本变化时自动识别旧结果并重新分析

---

### 二、舆情分析规则 2.0

#### 1. 分类误报修复

**原问题**：旧规则使用“买、卖、出、钱、火、打”等单字，只要命中任意一个词就分类，导致普通二手交易和日常讨论大量误报。

**优化内容**：
1. 改为带权短语、标题加权、来源排除和上下文组合
2. 普通二手商品不会因为“转账、充电、电动车、自行车”等词被判为诈骗、消防或交通事件
3. 诈骗分类需要“被骗、诈骗、收钱不发货、转账后失联”等明确证据
4. 消防、宿舍和餐饮分类增加真实场景或故障证据门槛
5. 新增分类置信度、细分话题和分析版本字段

---

#### 2. 中文歧义与网络用语修复

**抽样发现的问题**：
- “实习要不要跑路”误判为诈骗
- “考试爆炸”“游戏产能爆炸”误判为突发事件
- “救命，谁能借卫生纸”误判为突发或餐饮问题
- “入室抢劫的爱情”误判为治安事件
- “私发骚扰老师要求捞分”误判为人身骚扰
- 二手帖子中的“骗子别来”误判为诈骗事件

**解决方法**：增加真实事故上下文、餐饮场景、设施故障和普通二手交易排除规则。上述样例均已归为普通讨论。

---

#### 3. 风险评分与事件聚合升级

**优化内容**：
1. 风险评分综合类别严重程度、分类置信度、负面情绪、紧迫词、浏览量和互动量
2. 非安全帖子风险评分归零，避免高热度普通帖子变成高风险
3. 事件从“仅按安全大类聚合”改为“安全类别 + 细分话题 + 自然周”聚合
4. 使用稳定事件 ID，重复分析不会生成重复事件，并保留处置状态
5. 事件时间改为相关帖子实际发布时间，不再全部显示为系统分析时间

**最终全量验证结果（规则 2.0）**：
- 总帖子：9,753 条
- 明确安全相关：81 条
- 普通讨论：9,672 条
- 聚合事件：11 个
- 高风险帖子：1 条
- 中风险帖子：17 条
- 低风险帖子：9,735 条

---

### 三、数据与性能优化

#### 1. 万条数据性能适配

**优化内容**：
1. 帖子监测由“全部查询后在内存分页”改为数据库分页
2. 单次分页上限设为 200 条
3. 增量导入只查询已有帖子 ID，不再加载所有完整实体做去重
4. 开启 Hibernate 批量插入与批量更新
5. 重新分析和数据导入接口延长超时时间

---

#### 2. 数据管理与分析质量展示

**新增指标**：
- 安全相关帖子数
- 普通讨论数
- 安全内容占比
- 高置信分类占比
- 正文缺失数量
- 当前分析规则版本

同时修复了重新分析完成后前端统计卡被成功消息覆盖的问题。

---

### 四、任务二扩展接口

**背景**：任务二分析标准尚未收到，不能提前猜测评分逻辑。

**解决方法**：
1. 新建统一的 `AnalysisTaskExtension` 扩展契约
2. 预留任务二状态与执行接口
3. 明确可使用的帖子标准化字段
4. 当前状态为 `WAITING_FOR_STANDARD`
5. 任务二未启用时不会影响核心分析结果

**接口**：
- `GET /api/analysis/extensions/task2`
- `POST /api/analysis/extensions/task2/run`

---

### 五、系统设置持久化漏洞修复

#### 1. 分类刷新后消失

**问题描述**：前端虽然提交新增分类，但后端 `PUT /api/settings` 只返回“保存成功”，没有保存请求；刷新后 `GET /api/settings` 又返回硬编码默认分类。

**解决方法**：
1. 新增 `system_settings` 数据表
2. 风险阈值、分类和自定义关键词以 JSON 形式持久化到 H2
3. 刷新页面和重启后端后仍可读取已保存设置
4. 增加重复分类、空分类、关键词长度和阈值关系校验

---

#### 2. 设置未参与实际分析

**原问题**：风险阈值只在设置页显示，没有参与帖子和事件分级；新增分类也没有分析规则。

**解决方法**：
1. 风险阈值真正参与帖子、事件及人工研判分级
2. 新增自定义分类时必须填写至少一个不少于 2 个字符的识别关键词
3. 自定义分类关键词参与后续帖子分析
4. 删除内置分类可停用对应识别规则
5. 保存设置后自动重新分析全部帖子和事件
6. 舆情监测与事件管理的分类筛选改为动态读取系统设置
7. 修复“其他”分类无法筛选的问题

**持久化验证**：使用临时分类完成“保存 → 后端重启 → 重新读取”测试，确认设置仍存在；验证结束后已清除测试分类并恢复默认 70/40 阈值。

---

### 六、测试、启动与安全检查

#### 1. 构建与测试

- 后端自动化测试：16 项全部通过
- 前端 TypeScript 与正式构建：通过
- 全量 9,753 条数据内存库验证：通过
- 分页、筛选、设置保存、自动重分析、任务二接口：通过
- 项目启动地址：前端 `http://localhost:5173`，后端 `http://localhost:8080/api`

---

#### 2. GitHub 提交前信息安全审计

**检查范围**：待提交源码、配置、测试、文档、Git 历史和远程地址。

**检查结果**：
1. 待提交文件中未发现 API Key、访问令牌、密码、私钥或带凭证的远程地址
2. 待提交文件中未发现邮箱、手机号或身份证号等个人联系方式
3. `package-lock.json` 的依赖完整性哈希产生一次规则误报，确认不是密钥
4. Git 历史中未发现原始帖子数据或本地数据库
5. 发现旧提交作者元数据使用了真实姓名和 QQ 邮箱；因远程仓库为私密仓库并需保留原版本，已恢复原 3 个提交及其哈希，不再改写原提交身份；今天新增提交使用 GitHub 用户名及 `users.noreply.github.com` 匿名地址
6. `posts(2).json` 含用户昵称、头像地址和帖子正文，不纳入 Git
7. `backend/data/` 含本地 H2 完整数据，不纳入 Git
8. `.claude/`、环境变量文件、数据库文件和原始抓取数据已加入 `.gitignore`

**推送范围**：仅推送安全的源代码、自动化测试、项目说明、依赖清单和沟通记录。

---

## 2026-07-18

### 一、项目启动与联调环境

#### 1. 前后端启动

**沟通需求**：先启动项目，随后补充要求后端也同时启动，便于现场查看并持续提出前端调整意见。

**处理结果**：
1. 启动 Vue 前端开发服务，地址为 `http://127.0.0.1:5173`
2. 打包并启动 Spring Boot 后端，API 地址为 `http://127.0.0.1:8080/api`
3. 后端启动后完成 9,753 条帖子和 12,779 条评论的增量同步检查
4. 保持开发服务运行，使前端修改可通过热更新直接查看

---

#### 2. 登录失败与服务重启排查

**用户反馈**：新增登录后暂时无法登录，要求重新启动前后端。

**根本原因**：
1. 当时 5173 和 8080 端口均没有有效服务
2. 重新启动前端时发现 5173 被旧 Node 进程残留占用，新服务自动切换到了 5174

**解决方法**：
1. 清理占用 5173、5174 的残留前端进程
2. 将前端固定重新启动到 5173
3. 重新打包并启动后端到 8080
4. 使用真实接口完成联调验证：前端首页返回 200、未登录接口返回 401、正确登录返回 200、登录状态查询返回 200

---

### 二、全局页面排版与可读性优化

#### 1. 侧边栏字体与导航布局

**用户反馈**：侧边栏字体偏小、位置过于靠左，与整体页面观感不协调；随后进一步明确需要放大后四个目录“详细内容”的字体，而不只是目录名称。

**解决方法**：
1. 放大侧边栏导航文字和图标
2. 导航项内容改为居中排列，统一图标与文字间距
3. 放大事件管理、报告中心、系统设置、数据管理四个页面的正文、表格、徽章和辅助说明字体
4. 保留页面信息密度，同时提高大屏环境下的可读性

---

#### 2. Logo 区重新设计

**用户反馈**：原 Logo 和文字尺寸与页面整体风格不匹配。

**解决方法**：
1. 重做侧边栏顶部品牌区
2. 使用统一的盾牌矢量图标、科技感边框和状态点
3. 调整“校园安全舆情”和副标题的字号、间距与字重
4. 与深色渐变侧栏、导航图标和主页面字体体系保持一致

---

#### 3. 各页面顶部标题位置统一

**用户反馈**：舆情总览、舆情监测、事件管理等页面顶部标题过于靠左顶格，希望一次性解决所有页面。

**沟通确认**：用户特别澄清此项只修改页面顶部标题，不应联动修改其他矩形卡片。

**解决方法**：
1. 将顶部标题缩进统一收口到主布局的 `.app-header`
2. 桌面端统一设置左右内边距，移动端保留响应式间距
3. 撤回与顶部标题无关的卡片范围修改，避免影响其他矩形框
4. 后续卡片间距作为独立需求单独处理

---

#### 4. 卡片间距与页面留白统一

**用户反馈**：总览页顶部统计卡上方和左右有间距，但与下方趋势卡片之间没有间隔，风格不统一。

**解决方法**：
1. 统一总览页各区域的纵向间距
2. 使顶部统计卡与下方趋势区域之间保留与左右一致的视觉留白
3. 继续保持卡片直角设计，不重新引入圆角

---

#### 5. 字体颜色与背景对比度

**用户反馈**：部分日期、辅助文字和帖子内容颜色过浅，与背景对比不明显。

**解决方法**：
1. 加深日期和辅助信息颜色
2. 统一浅色背景上的次级文字对比度
3. 为深色侧栏单独保留更明亮的辅助文字颜色，避免全局颜色规则影响侧栏
4. 放大舆情监测中的帖子标题、正文和元信息字体

---

#### 6. 搜索框与选择框

**用户反馈**：搜索框、筛选选择框上下过窄，文字较小，界面显得拥挤。

**解决方法**：
1. 统一输入框和选择框的最小高度
2. 增加上下内边距
3. 放大输入文字和筛选文字
4. 保持搜索框、类型、情绪、分类筛选控件的高度一致

---

### 三、舆情总览与趋势分析合并

#### 1. 功能合并与目录去重

**用户反馈**：舆情总览与趋势分析功能存在重合，项目负责人要求合并去重并放在同一目录。

**解决方法**：
1. 将趋势研判内容并入舆情总览页面
2. 侧边栏不再单独显示“趋势分析”
3. 保留旧 `/trends` 地址并重定向到 `/dashboard`，避免旧链接失效
4. 趋势图继续使用后端真实数据，不重复保留同类统计模块

---

#### 2. 趋势研判标题和时间范围

**用户反馈**：趋势研判标题位置需要调整；7天、30天、自定义时间范围点击无效。

**解决方法**：
1. 调整趋势研判标题和时间筛选区的内边距及对齐
2. 补齐 7 天、30 天和自定义时间范围的交互状态
3. 时间范围变化后重新请求对应数据并更新图表
4. 保留刷新入口，用于主动重新加载当前范围

---

### 四、帖子与评论后端关联

#### 1. 评论数据导入与关联

**用户需求**：修改后端，将帖子和评论连接起来，点击帖子后能够查看该帖评论。

**解决方法**：
1. 新增评论实体、数据仓库、导入服务和启动初始化器
2. 默认读取项目根目录的 `comments.json`
3. 评论以 `comment_id` 增量同步，通过 `thread_id` 与帖子 `id` 关联
4. 未匹配评论继续保留，但不参与帖子风险评判
5. 数据库不保存评论者昵称、头像及用户标识，仅保留正文和分析必要字段

**数据验证**：
- 评论总数：12,779 条
- 已关联评论：12,057 条
- 未匹配评论：722 条
- 已覆盖帖子：1,755 条

---

#### 2. 帖子详情与评论查看

**解决方法**：
1. 新增帖子详情路由和 `PostDetail.vue`
2. 舆情监测列表中的帖子支持点击进入详情
3. 新增按帖子 ID 分页读取评论的接口
4. 详情页展示原帖信息、分析结果、评论列表和评论风险依据
5. 返回列表时保留清晰的页面层级

**新增接口**：
- `GET /api/comments/post/{postId}`

---

#### 3. 评论辅助评判规则 2.1.3

**处理原则**：
1. 评论不会直接覆盖原帖的安全分类
2. 多条同类风险评论可形成“建议人工复核”提示
3. 已分类帖子可根据评论中的同类证据、负面比例、互动量和紧急表达获得有限风险加权
4. 评论结果单独展示，便于管理人员核对分析来源

---

### 五、系统设置与任务二接口调整

#### 1. 删除手动风险阈值

**用户需求**：系统设置中不再手动量化高、中、低风险分数；任务二同学只传递低风险、中风险、高风险标记。

**解决方法**：
1. 删除系统设置页的风险阈值调节界面
2. 后端设置接口不再依赖人工配置的分数阈值
3. 任务二扩展契约改为只接收离散风险等级
4. 风险等级限定为低、中、高，不传递分数或阈值
5. 任务二结果作为独立扩展，不影响现有核心分析流程

**任务二数据约定**：

```json
{
  "results": [
    { "postId": "帖子ID", "riskLevel": "低" }
  ]
}
```

---

### 六、报告中心功能修复

#### 1. 日报与周报切换

**用户反馈**：报告中心只能看到日报，日报、周报无法调整。

**解决方法**：
1. 补齐日报、周报类型切换控件
2. 前端选择报告类型后携带对应参数请求后端
3. 后端根据 `daily`、`weekly` 分别计算报告周期
4. 报告列表、详情预览和导出内容同步使用当前报告类型
5. 保留事件专题报告类型的后端支持

---

### 七、智能助手与侧边栏交互

#### 1. 智能助手改为悬浮模式

**用户需求**：不再将智能助手作为普通侧边栏目录，改成悬浮入口。

**解决方法**：
1. 从侧边栏隐藏独立的智能助手菜单项
2. 在页面右下角增加全局悬浮助手按钮
3. 保留在线状态提示和展开交互
4. 旧 `/assistant` 地址重定向到总览，避免出现重复入口

---

#### 2. 大模型风格的侧边栏收起效果

**用户需求**：参考大模型聊天界面的侧栏收起方式。

**解决方法**：
1. 删除侧边栏底部的“收起”文字按钮
2. 在页面顶部左侧增加面板切换图标
3. 点击后整个侧边栏平滑滑出，主内容区同步扩展
4. 收起后保留展开按钮，可随时恢复侧边栏
5. 增加悬停、按压和宽度变化动画
6. 移动端继续使用原有抽屉式菜单逻辑

---

### 八、最简昵称与密码登录

#### 1. 后端登录与会话

**用户需求**：更新前后端设置，先实现最简单的昵称和密码登录。

**解决方法**：
1. 新增登录、当前用户和退出登录接口
2. 登录成功后生成随机会话令牌，默认有效期 24 小时
3. 新增后端认证拦截器，保护 `/api/**` 接口
4. 未携带有效 Bearer Token 的请求统一返回 401
5. 当前本地演示账号为“管理员”，密码为 `123456`

**新增接口**：
- `POST /api/auth/login`
- `GET /api/auth/me`
- `POST /api/auth/logout`

> 当前账号和密码为本地演示配置，正式部署前应替换，并改用加密密码存储和持久化用户表。

---

#### 2. 前端登录状态

**解决方法**：
1. 新增独立登录页面
2. 登录成功后保存令牌和昵称
3. 所有 API 请求自动携带 Bearer Token
4. 增加路由守卫，未登录时自动进入登录页
5. 接口返回 401 时清理失效状态并重新登录
6. 顶栏展示当前昵称，并增加退出登录按钮

---

#### 3. 添加登录后侧边栏目录消失

**用户反馈**：登录后原来的页面仍在，但侧边栏多个目录全部消失。

**根本原因**：新增登录页后路由顺序发生变化，侧边栏仍固定读取路由数组第一项，实际读到没有子路由的登录页。

**解决方法**：
1. 不再依赖路由数组顺序
2. 按主布局路径 `/` 精确查找导航子路由
3. 恢复舆情总览、舆情监测、事件管理、报告中心、系统设置和数据管理目录
4. 趋势分析继续按需求合并到总览，智能助手继续保持悬浮模式

---

### 九、当日验证结果

- 前端 TypeScript 检查与正式构建：通过
- 后端 Maven 自动化测试：20 项全部通过
- 后端可执行包构建：通过
- 前端 5173 服务：启动并返回 200
- 后端 8080 服务：启动成功
- 未登录访问受保护接口：返回 401
- 正确昵称和密码登录：返回 200
- 登录状态查询：返回 200，并正确识别“管理员”
- 会话令牌长度：32 位
- 侧边栏目录修复：开发服务热更新生效
- 大模型风格侧栏动画：前端构建通过并热更新生效

---

### 十、当日主要涉及文件

**前端**：
- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/router/index.ts`
- `frontend/src/pages/Login.vue`
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/Monitoring.vue`
- `frontend/src/pages/PostDetail.vue`
- `frontend/src/pages/Reports.vue`
- `frontend/src/pages/Settings.vue`
- `frontend/src/pages/EventList.vue`
- `frontend/src/pages/DataManage.vue`
- `frontend/src/components/AppIcon.vue`
- `frontend/src/components/FloatingAssistant.vue`
- `frontend/src/style.css`
- `frontend/src/utils/api.ts`

**后端**：
- `backend/src/main/java/com/nankai/yuqing/controller/AuthController.java`
- `backend/src/main/java/com/nankai/yuqing/service/AuthService.java`
- `backend/src/main/java/com/nankai/yuqing/config/AuthInterceptor.java`
- `backend/src/main/java/com/nankai/yuqing/config/CorsConfig.java`
- `backend/src/main/java/com/nankai/yuqing/controller/CommentController.java`
- `backend/src/main/java/com/nankai/yuqing/service/CommentImportService.java`
- `backend/src/main/java/com/nankai/yuqing/config/CommentDataInitializer.java`
- `backend/src/main/java/com/nankai/yuqing/controller/ReportController.java`
- `backend/src/main/java/com/nankai/yuqing/controller/SettingsController.java`
- `backend/src/main/java/com/nankai/yuqing/service/Task2AnalysisExtension.java`
- `backend/src/main/resources/application.yml`

---

## 2026-07-19

### 一、全局页面间距统一

#### 1. 顶部标题、筛选栏与正文对齐

**用户反馈**：舆情监测页面顶部标题与搜索筛选栏之间没有合理间隔，各页面上下左右留白不统一，整体观感不协调。

**根本原因**：
1. 主内容区已经设置页面外边距；
2. `.page` 容器又叠加左右 `32px`；
3. 卡片内部仍使用固定的左右 `52px`；
4. 三层间距形成不同的内容对齐线。

**解决方法**：
1. 将页面外边距统一收口到主布局；
2. 桌面端页面四周统一为 `32px`，移动端为 `16px`；
3. 页面区块间距统一为 `24px`；
4. 卡片内边距统一为上下 `24px`、左右 `32px`；
5. 删除 `.page` 重复叠加的左右边距。

**验证结果**：前端生产构建通过，所有主要页面共用同一套间距变量。

---

### 二、报告导出格式修复

#### 1. PDF 按钮实际导出 TXT

**用户反馈**：报告中心点击“导出 PDF”后下载的是 TXT 文件。

**根本原因**：旧实现始终创建 `text/plain` Blob，PDF 分支还将扩展名写成 `.txt`，仅属于改文件名，并未生成 PDF。

**解决方法**：
1. 使用 `html2canvas` 将中文报告按页面样式渲染；
2. 使用 `jsPDF` 生成真实 PDF；
3. 按 A4 尺寸自动分页；
4. 保留报告标题和中文正文排版；
5. 导出期间显示“正在生成”，避免重复点击；
6. PDF 依赖改为点击导出时动态加载；
7. Word 导出同步改为有效 `.doc` 内容，不再使用伪装扩展名的纯文本。

**验证结果**：前端构建通过，PDF 文件格式、扩展名和中文内容正常。

---

### 三、整体功能架构评估与分阶段计划

#### 1. 沟通结论

用户要求从系统整体架构和前端展示角度提出改进建议，并确认采用“一个方向完成后先测试，再继续下一个方向”的方式推进。

最终确定七个阶段：

1. 事件处置闭环；
2. 人工复核与误报处理；
3. 风险规则和触发依据；
4. 监测列表信息密度优化；
5. 首页工作台改造；
6. 报告结构化预览和正式版式；
7. 数据质量监控与权限审计。

推进原则：

- 每次只实施一个阶段；
- 完成前后端修改和验证后暂停；
- 用户测试确认后再进入下一阶段；
- 不以增加页面数量为目标，优先完善业务闭环、数据可信度和操作效率。

---

### 四、第一阶段：事件处置闭环

#### 1. 原有问题

事件详情虽然存在“保存研判”入口，但后端只保存状态和风险；负责人、截止时间、备注和处置历史均未落库，页面处置记录为模拟数据。

#### 2. 完成内容

1. 状态统一为待核实、处理中、持续观察、已解决、误报；
2. 兼容待研判、已确认、已忽略等历史状态；
3. 新增负责人和计划完成时间；
4. 自动识别未关闭的超时事件；
5. 已解决或误报时必须填写处置结论；
6. 每次保存生成真实处置记录；
7. 负责人、截止时间、结论和记录持久化；
8. 事件列表增加负责人、计划完成时间和超时状态。

#### 3. 沟通过程

依赖下载阶段出现短暂等待，用户询问网络状态后确认继续工作；随后完成本地后端测试和前端构建。用户按测试清单确认第一阶段没有问题，并同意进入第二阶段。

**验证结果**：后端 20 项测试全部通过，前端类型检查和生产构建通过。

---

### 五、第二阶段：人工复核与误报处理

#### 1. 设计原则

检查发现帖子只有 AI 分析字段。如果人工操作直接覆盖原字段，将无法比较 AI 与人工差异。因此采用“AI 原始结果永久保留、人工最终结论独立保存”的方式。

#### 2. 完成内容

1. 新增待复核、已确认、已修正、无关内容四种状态；
2. 监测列表支持按复核状态筛选；
3. 帖子详情新增人工复核区域；
4. 支持确认 AI、修改分类/情绪/风险、标记无关内容；
5. 修正和标记无关时强制填写原因；
6. 保存复核人、复核时间和复核说明；
7. 列表与报告优先使用人工最终结论；
8. AI 原始结论继续保留用于差异核对；
9. 新增撤销复核功能，可重新进入待复核队列。

#### 3. 人工复核无法保存

**用户反馈**：新复核界面可以打开，但保存功能无法正常使用。

**排查结果**：当前 `8080` 端口仍运行旧版后端，新接口 `/api/posts/{id}/review` 返回 `404`；问题不在前端表单或复核逻辑。

**解决方法**：
1. 停止旧后端并启动新版后端；
2. 使用用户截图中的帖子进行真实保存测试；
3. 验证分类、风险、复核人、说明和时间均写入数据库；
4. 测试后将帖子恢复为待复核，未保留测试数据；
5. 补充“撤销复核”入口；
6. 提醒后端重启后登录令牌失效，需要刷新并重新登录。

**验证结果**：真实接口返回 `200`；后端 21 项测试全部通过；前端生产构建通过；数据库筛选专项测试通过。

---

## 2026-07-20

### 一、已完成与未完成方向复核

#### 1. 沟通需求

用户要求恢复到校园集市舆情项目的上下文，并核查此前提出的前后端建议哪些已完成、哪些尚未完成。

#### 2. 核查结论

1. 事件处置闭环：核心完成；
2. 人工复核与误报处理：核心完成；
3. 风险规则和触发依据：当时部分完成；
4. 监测列表信息密度优化：当时基本未完成；
5. 首页工作台改造：部分完成；
6. 报告结构化预览：部分完成，真实 PDF 已完成；
7. 数据质量监控与权限审计：仅完成少量基础统计。

用户确认继续第三阶段，后续仍按“完成一个阶段后暂停测试”的方式推进。

---

### 二、第三阶段：风险规则和触发依据

#### 1. 初版预警规则

初版在原有分析基础上增加独立预警规则层，包括讨论量、负面比例、互动量、浏览量、短时间突增、重复地点和高危关键词，并在首页和事件详情展示触发原因。

联调过程中解决了两个兼容问题：

1. 保存设置后重新聚合事件可能丢失负责人、截止时间和处置结论，现已改为保留；
2. “未明确、未知、暂无”等占位地点会形成无效重复地点预警，现已过滤。

#### 2. 删除风险可调分数

**用户补充说明**：第二位同学不会传递风险分数，只会提供 AI 自主判断的低风险、中风险、高风险标签；因此要求删除可调分数，保留人工修改判定结果。

**最终调整**：
1. 删除系统设置中的风险分数预警线；
2. 页面和帖子接口不再展示或返回 `riskScore`；
3. 预警直接采用低/中/高离散标签；
4. 讨论量、负面比例、传播量等只作为解释依据，不改变风险标签；
5. 支持导入 `ai_risk_level`、`risk_level`、`riskLevel`、`risk_label`；
6. 外部 AI 标签单独保存，重新分析不会覆盖；
7. 重复导入同一帖子时允许补充或更新外部风险标签；
8. 最终判定优先级为人工判定、外部 AI、本地分析；
9. 人工修改或撤销后立即重新聚合事件和首页预警；
10. 即使没有分数和安全分类，只要外部标签为中风险或高风险，也能进入预警链路；
11. 标签来源在页面区分为“外部AI”或“本地分析”。

**验证结果**：

- 后端 25 项测试全部通过；
- 前端生产构建通过；
- 设置接口确认不存在风险分数阈值；
- 首页预警首要依据为 `risk_label`；
- 帖子接口不再返回风险分数。

---

### 三、第四阶段：舆情监测列表优化

#### 1. 完成内容

1. 帖子正文默认显示两行，可单独展开和收起；
2. 支持本页全选和多选；
3. 支持批量确认 AI；
4. 支持批量标记无关内容，并校验原因；
5. 批量操作完成后统一重新聚合事件和首页预警；
6. 新增最新发布、风险优先、热度优先三种排序；
7. 风险排序使用最终标签，遵循人工、外部 AI、本地分析的优先级；
8. 热度排序综合浏览量、评论量和点赞量；
9. 排序在数据库分页层执行，避免加载全部帖子后再排序；
10. 支持命名、保存、应用和删除常用筛选方案；
11. 补充兼职招聘来源筛选。

#### 2. 快速详情抽屉改回完整详情页

**用户反馈**：右侧快速详情抽屉压缩了长正文和评论，文字排版与主页面不一致；要求恢复原来直接进入完整帖子页面的交互。

**解决方法**：
1. 完全删除右侧快速详情抽屉；
2. 点击帖子直接进入完整帖子详情页；
3. 保留正文展开、批量操作、排序和筛选方案；
4. 完整详情页统一标题字号、正文行距、文字颜色和卡片间距；
5. 补充浏览量、评论量、点赞量、地点、问题和诉求信息；
6. 人工复核与评论区域统一文字层级；
7. 进入帖子详情后，侧边栏继续高亮“舆情监测”。

**验证结果**：

- 后端 27 项测试全部通过；
- 新增风险排序和热度排序专项测试；
- 前端生产构建通过；
- 9,753 条真实数据分页正常；
- 风险排序实测为高风险到中风险；
- 热度排序前五项按热度值递减；
- 联调只验证批量接口参数校验，未修改现有帖子；
- 前端与后端服务均正常运行。

---

### 四、两日沟通与实施原则总结

1. 用户通过截图反馈具体页面问题，修改以实际界面和真实接口结果为准；
2. 每个优化方向完成后暂停，提供测试清单，由用户确认后再继续；
3. 对用户补充的协作数据格式及时调整设计，不继续保留无来源的风险分数；
4. AI 原始结果与人工结论分离保存，保证结果可追溯；
5. 发现旧进程、旧接口或运行版本不一致时，先完成真实接口复现，再修改或重启；
6. 测试过程中避免污染现有帖子数据，确需保存验证时及时恢复；
7. 所有阶段均执行前端生产构建和相应后端自动化测试；
8. 截至 2026-07-20，第一至第四阶段已完成，第五阶段“首页工作台改造”尚未开始。

---

## 2026-07-21

### 一、第五阶段：首页工作台改造

#### 1. 完成内容

1. 首页新增“今日工作台”，将待复核帖子、待处理预警、截止事项和数据状态放在趋势图表之前；
2. 待复核队列按最终风险标签、传播热度和发布时间排序，并可直接进入帖子完整详情；
3. 中高风险且未关闭的事件计入待处理预警，已解决和误报事件不再占用工作台；
4. 汇总已超时和未来 48 小时内到期的事件，展示负责人和截止时间；
5. 增加今日与昨日的新增帖子、中高风险标签和负面情绪占比对比；
6. 增加最新数据时间、数据新鲜度和正文缺失数量提示；
7. 首页最近事件补充真实事件 ID，可直接进入事件详情。

**验证结果**：后端测试通过，前端生产构建通过，首页数据均来自真实接口。

---

### 二、第六阶段：报告结构化预览和正式版式

#### 1. 原有问题

报告预览仍以整段纯文本展示，缺少报告编号、数据范围和复核信息；PDF 虽为真实文件，但通过长截图自动切页，可能把段落或表格截断；Word 只保留纯文本内容，与页面预览不一致。

#### 2. 完成内容

1. 后端报告详情增加报告编号、报告类型、生成时间、数据范围和保密级别；
2. 增加监测帖子、事件、高风险事件和负面帖子结构化概览；
3. 增加总体情况、安全议题分布、情绪构成、重点事件、相关讨论和处置建议字段；
4. 日报、周报和事件简报共用统一的正式报告结构；
5. 页面预览改为两页 A4 版式，包含页眉、标题区、元数据表、统计表格、重点事件、建议、复核栏和页码；
6. PDF 改为按完整报告页逐页渲染，避免在段落和表格中间切断；
7. Word 导出复用相同章节和表格样式，不再输出纯文本预览；
8. 复制文本功能继续保留，便于快速粘贴报告正文；
9. 为保证正式两页版式稳定，重点事件按风险优先展示前 5 起，相关讨论展示前 4 条。

#### 3. 验证结果

- 后端自动化测试：27 项全部通过；
- 前端 TypeScript 检查和生产构建：通过；
- 新后端已重新打包并启动在 `8080`；
- 真实日报接口已验证包含 `meta`、`overview`、`keyEvents`、`recommendations` 等结构化字段；
- 生成两页 A4 样例 PDF，并逐页转为图片检查；表格、建议区、复核栏和页脚均无截断或重叠。

---

### 三、第七阶段：数据质量监控与权限审计

#### 1. 原有问题

1. 数据管理页仅展示数据总量，无法集中看到异常类型、严重程度和处理建议；
2. 登录会话只判断是否登录，没有明确角色和权限清单；
3. 人工复核、事件处置、系统设置、数据导入、重新分析和清空数据等关键操作缺少统一审计记录。

#### 2. 数据质量监控

1. 新增 0—100 分的数据质量评分；
2. 从内容完整性、分析覆盖率、评论关联率和数据时效性四个维度计算；
3. 自动检查正文缺失、标题与正文同时缺失、发布时间缺失、分析版本落后、风险标签不规范、评论未匹配和数据更新滞后；
4. 每项异常展示高、中、低级别、影响数量、问题说明和处理建议；
5. 数据管理页展示最近数据时间和本次检查时间；
6. 当前真实数据评分为 87 分，识别到正文缺失 671 条、未匹配评论 722 条以及数据更新滞后问题。

#### 3. 权限与审计

1. 登录结果和当前用户接口增加角色与权限清单；
2. 当前演示账号明确为 `ADMIN` 管理员角色；
3. 权限划分为查看数据、人工复核、事件处置、系统设置、数据管理和查看审计；
4. 数据写操作与系统设置写操作在后端执行权限校验，不能仅依赖前端隐藏按钮；
5. 新增持久化审计表和分页查询接口；
6. 自动记录人工复核、批量复核、事件处置、系统设置、帖子导入、评论导入、重新分析、清空数据、扩展分析和退出登录；
7. 审计记录包含操作人、角色、操作类型、业务对象、执行结果、来源类别和时间；
8. 不记录密码、登录令牌、请求正文或原始网络地址；
9. 数据管理页支持按操作类型筛选审计记录，并显示 24 小时操作数和失败数。

#### 4. 验证结果

- 后端自动化测试：29 项全部通过；
- 新增管理员角色、权限和无效登录专项测试；
- 前端 TypeScript 检查与生产构建：通过；
- 真实登录接口返回管理员角色和 6 项权限；
- 真实数据质量接口返回评分、四项维度和异常明细；
- 使用安全的退出登录操作验证审计记录成功持久化；
- 审计记录敏感信息检查通过；
- 第七阶段完成，原定七个优化阶段全部实施完毕。

---

## 七阶段优化任务完成情况总汇总

### 一、总体结论

原定七个阶段的核心任务现已全部完成，系统已从基础数据展示升级为包含“自动分析、人工复核、事件处置、风险解释、工作台、正式报告、质量监控和操作审计”的完整业务闭环。

整体业务流程如下：

帖子与评论导入 → AI 分类、情绪和风险标签 → 风险依据解释 → 人工复核 → 安全事件聚合 → 负责人处置 → 首页跟踪 → 正式报告 → 数据质量与操作审计。

---

### 二、第一阶段：事件处置闭环

**完成目标**：解决事件只能查看、无法持续跟踪处置的问题。

**已实现功能**：

1. 统一事件状态为待核实、处理中、持续观察、已解决和误报；
2. 支持填写负责人、计划完成时间、处置备注和最终结论；
3. 已解决或误报时必须填写处置结论；
4. 自动判断事件是否超时；
5. 每次状态变化生成真实处置记录；
6. 负责人、截止时间、结论和处置历史持久化保存；
7. 事件列表和详情同步展示负责人、截止时间及超时状态；
8. 重新分析和重新聚合事件时保留已有人工处置信息。

**形成的闭环**：事件发现 → 指派负责人 → 设置时限 → 记录过程 → 填写结论 → 关闭或标记误报。

---

### 三、第二阶段：人工复核与误报处理

**完成目标**：让工作人员能够修正 AI 结果，同时保留原始分析用于追溯。

**已实现功能**：

1. 增加待复核、已确认、已修正和无关内容四种复核状态；
2. 支持确认 AI 原始结论；
3. 支持人工修改安全分类、情绪和低、中、高风险标签；
4. 支持将普通内容标记为无关内容；
5. 修正和标记无关内容时必须填写原因；
6. 保存复核人、复核时间和复核说明；
7. AI 原始结果与人工最终结论分开保存；
8. 列表、事件和报告优先使用人工最终结论；
9. 支持撤销复核，帖子可重新进入待复核队列；
10. 支持按复核状态筛选帖子。

**形成的闭环**：AI 判断 → 人工确认或修正 → 保存依据 → 下游统一使用最终结论 → 必要时撤销重审。

---

### 四、第三阶段：风险规则和触发依据

**完成目标**：删除无数据来源的可调风险分数，改为使用离散风险标签并提供可解释依据。

**已实现功能**：

1. 删除系统设置中的高、中、低风险分数调节；
2. 风险数据统一使用低风险、中风险和高风险标签；
3. 支持导入 `ai_risk_level`、`risk_level`、`riskLevel` 和 `risk_label`；
4. 外部 AI 风险标签独立保存，重新分析不会覆盖；
5. 重复导入帖子时允许补充或更新外部 AI 标签；
6. 最终风险优先级为人工判定、外部 AI、本地分析；
7. 讨论量、负面比例、浏览量、互动量、短期增长、地点和高危词只作为解释依据，不再修改风险标签；
8. 首页预警和事件详情展示触发原因；
9. 过滤未明确、未知、暂无等无效地点；
10. 即使没有本地风险分数，只要外部标签为中或高，也能进入预警链路；
11. 页面明确区分外部 AI 和本地分析来源。

**最终数据约定**：第二位同学只需传递帖子 ID 和低、中、高风险标签，无需提供风险分数。

---

### 五、第四阶段：舆情监测列表优化

**完成目标**：提高万条数据环境下的浏览、筛选、排序和批量复核效率。

**已实现功能**：

1. 帖子正文默认显示两行，可单独展开和收起；
2. 点击帖子直接进入完整详情页，不再使用右侧快速详情抽屉；
3. 完整详情页统一标题、正文、元信息、评论和人工复核的排版；
4. 支持本页全选和多选；
5. 支持批量确认 AI；
6. 支持批量标记无关内容并校验原因；
7. 批量完成后统一重新聚合事件和预警；
8. 支持最新发布、风险优先和热度优先排序；
9. 风险排序遵循人工、外部 AI、本地分析优先级；
10. 热度排序综合浏览量、评论量和点赞量；
11. 排序和筛选在数据库分页层完成；
12. 支持保存、应用和删除常用筛选方案；
13. 支持兼职招聘等来源筛选；
14. 详情页补充浏览量、评论量、点赞量、地点、问题和诉求信息；
15. 进入帖子详情后侧边栏继续高亮舆情监测。

**性能结果**：9,753 条真实帖子可以正常分页、筛选和排序，无需一次性加载全部数据。

---

### 六、第五阶段：首页工作台改造

**完成目标**：把首页从单纯统计看板改为工作人员每天可以直接处理任务的行动工作台。

**已实现功能**：

1. 新增今日工作台；
2. 汇总待复核帖子数量；
3. 汇总中高风险且尚未关闭的待处理预警；
4. 汇总已超时及未来 48 小时内到期事件；
5. 展示数据更新异常数量；
6. 待复核队列按最终风险、传播热度和发布时间排序；
7. 工作台任务可直接跳转到对应帖子、事件和数据管理页面；
8. 增加新增帖子、中高风险标签和负面情绪占比的今日与昨日对比；
9. 展示最新数据时间、数据距今小时数和正文缺失量；
10. 首页最近事件补充真实事件 ID，可直接进入事件详情；
11. 已解决和误报事件不再占用待处理预警。

**首页使用顺序**：先处理待复核、预警和到期事项，再查看趋势和统计图表。

---

### 七、第六阶段：报告结构化预览与正式版式

**完成目标**：将纯文本报告升级为可以正式预览、复核和导出的标准报告。

**已实现功能**：

1. 日报、周报和事件简报使用统一结构；
2. 增加报告编号、报告类型、生成时间、监测周期、数据范围和保密标识；
3. 增加监测帖子、事件、高风险事件和负面帖子概览；
4. 结构化展示总体情况、安全议题分布和情绪构成；
5. 展示按风险优先排序的重点事件；
6. 展示相关讨论摘要和研判处置建议；
7. 增加人工复核人、复核日期和免责声明；
8. 页面预览改为两页 A4 正式版式；
9. PDF 按完整报告页逐页生成，不再从段落或表格中间切断；
10. Word 导出复用相同章节和表格风格；
11. 保留复制纯文本功能；
12. 生成两页 A4 样例并逐页检查，未发现截断、遮挡或重叠。

---

### 八、第七阶段：数据质量监控与权限审计

**完成目标**：让管理人员知道数据是否可信，并让关键操作具备权限约束和追溯能力。

**已实现功能**：

1. 新增 0—100 分数据质量评分；
2. 检查内容完整性、分析覆盖率、评论关联率和数据时效性；
3. 自动识别正文缺失、核心文字缺失、发布时间缺失、分析版本落后、风险标签不规范、评论未匹配和数据滞后；
4. 每项异常展示严重级别、数量、说明和处理建议；
5. 登录和当前用户接口返回角色与权限；
6. 当前管理员拥有查看数据、人工复核、事件处置、系统设置、数据管理和查看审计六项权限；
7. 数据和系统设置写操作在后端执行权限校验；
8. 新增持久化审计记录；
9. 审计覆盖人工复核、批量复核、事件处置、设置修改、帖子导入、评论导入、重新分析、清空数据、扩展分析和退出登录；
10. 审计支持操作类型筛选、分页和 24 小时操作统计；
11. 审计不保存密码、令牌、请求正文或原始网络地址；
12. 顶栏展示当前账号的管理员角色。

**当前真实质量结果**：评分 87 分；正文缺失 671 条；未匹配评论 722 条；数据更新时效需要关注。

---

### 九、七阶段最终验证结果

1. 后端自动化测试共 29 项，全部通过；
2. 前端 TypeScript 检查和生产构建通过；
3. 前后端真实接口联调通过；
4. 9,753 条帖子和 12,779 条评论可以正常加载与统计；
5. 评论通过 `thread_id` 与帖子 ID 关联，已关联评论参与辅助研判；
6. 外部 AI 风险标签、人工最终结论和本地分析结果的优先级已统一；
7. 人工复核、事件处置、首页工作台、报告和审计之间的数据链路已经打通；
8. 前端服务使用 `5173`，后端接口使用 `8080`；
9. 原定七个优化阶段不存在尚未完成的核心功能；
10. 后续工作属于界面细节打磨、真实使用反馈修复或新增业务需求。

---

## 2026-07-22

### 一、新增个人中心

**用户需求**：在现有单管理员账号基础上增加个人中心功能。

**完成内容**：

1. 新增个人中心隐藏路由，不占用侧边栏目录；
2. 点击顶部头像、昵称或管理员标签可进入个人中心；
3. 展示当前昵称、账号类型、角色和头像首字；
4. 展示查看数据、人工复核、事件处置、系统设置、数据管理和查看审计六项权限；
5. 展示登录时间、最近活动时间、会话失效时间和剩余有效时间；
6. 展示当前账号自己的最近关键操作记录；
7. 个人中心提供返回首页和退出登录入口；
8. 个人操作记录继续使用现有审计数据，不额外保存敏感信息；
9. 顶部原有独立退出按钮保留，便于快速退出。

**接口调整**：

- 新增 `GET /api/auth/profile`，返回当前账号资料和会话信息；
- 新增 `GET /api/audit/my`，分页返回当前账号自己的审计记录。

**验证结果**：

- 前端 TypeScript 检查与生产构建通过；
- 后端 29 项自动化测试全部通过；
- 真实登录后成功读取管理员资料、六项权限、会话剩余时间和个人操作记录；
- 更新后的后端已重新启动并运行在 `8080`。

---

## 2026-07-25—2026-07-26

### 一、首页安全议题环形图显示修复

**用户反馈**：

1. 鼠标移动到不同扇形时，悬浮提示与图例、中心文字出现重叠；
2. 调整后环形图中心的“总议题 / 8类”文字又发生偏移。

**原因分析**：

- 环形图中心文字的位置依赖图表容器，而饼图本体为给右侧图例让位进行了偏移；
- 悬浮提示未充分限制位置，在较窄卡片中会压住图例或中心说明；
- 中心标题和饼图圆心未使用同一组定位参数。

**优化方案**：

1. 统一饼图圆心和中心文字的定位基准；
2. 将中心文字固定在圆环真实中心，不跟随图例区域变化；
3. 调整提示框定位、边距和层级，避免覆盖中心文字与图例；
4. 保持鼠标悬浮扇形时只突出当前数据，不产生重复标签。

**结果**：中心文字恢复居中，切换不同扇形时不再出现文字叠加。

---

### 二、真实数据状态与页面交互统一

**问题描述**：部分页面在接口失败或返回空数据时仍展示演示案例，容易把预置内容误认为真实舆情；全局刷新、错误状态和空状态也不统一。

**完成内容**：

1. 总览、事件、报告和系统设置移除演示数据回退；
2. 接口失败时展示明确错误状态和重试入口；
3. 空数组保持真实空状态，不再自动填充案例；
4. 新增统一错误状态组件；
5. 顶部刷新由整页重载改为仅刷新当前页面组件；
6. 分页、导航和报告类型切换改用真实按钮；
7. 图表补充可访问性描述；
8. 弹窗支持 `Esc` 关闭；
9. 增加减少动画偏好支持。

**效果**：页面展示口径与后端真实状态一致，局部刷新更快，错误恢复路径更明确。

---

### 三、数据管理可视化导入

**问题描述**：原数据管理页只提示调用 API，不适合管理员日常操作，也缺少导入前校验和结果反馈。

**完成内容**：

1. 支持选择帖子或评论 JSON 文件；
2. 校验文件扩展名、JSON 顶层数组和必填 ID；
3. 评论额外校验 `thread_id`；
4. 展示文件名、总条数和前 5 条数据预览；
5. 检测同一文件中的重复 ID，并提前提示会安全合并；
6. 导入结果分别展示总数、新增、更新、跳过、错误和合并重复数；
7. 未匹配评论明确提示“数据已保留，等待对应帖子”；
8. 评论导入后展示后台重新分析进度。

---

### 四、认证、账号与接口配置优化

**问题描述**：默认账号密码、纯内存会话和写死的前端接口地址只适合演示环境。

**完成内容**：

1. 前端 API 地址支持 `VITE_API_BASE_URL` 环境配置；
2. 开发环境通过 `/api` 代理访问后端；
3. 密码改为哈希后持久化保存；
4. 登录会话持久化，服务重启后仍可恢复；
5. 连续登录失败 5 次后锁定 15 分钟；
6. 登录锁定返回 HTTP 429；
7. 个人中心增加修改密码功能；
8. H2 控制台默认关闭；
9. 后端默认认证配置支持环境变量覆盖。

**说明**：联调时默认密码返回 401，表明数据库中管理员密码已经修改并持久化；未擅自重置账号。

---

### 五、舆情监测与事件处置优化

#### 1. 舆情监测

1. 搜索输入增加 300ms 防抖；
2. 新搜索会取消旧请求，避免旧响应覆盖新结果；
3. 搜索、筛选、排序和分页同步到 URL；
4. 返回列表后可恢复原筛选状态；
5. 评论数统一使用实际已关联评论数量。

#### 2. 事件处置

1. 负责人支持部门建议和自由输入；
2. “处理中”等状态要求填写负责人和计划完成时间；
3. 截止时间不能早于当前时间；
4. 增加状态流转规则；
5. 已解决或误报必须填写处置结论；
6. 离开页面前提示未保存修改；
7. 生成简报后先预览，再由用户确认复制；
8. 增加乐观并发控制：保存时携带事件更新时间；
9. 若其他管理员已先保存，当前提交返回 409，不覆盖对方结果。

---

### 六、重新分析任务后台化

**问题描述**：全量重新分析同步等待时间过长，数据增加后容易出现页面超时、重复点击或用户误判失败。

**优化方案**：

1. 新增单实例后台重新分析任务；
2. 提供任务 ID、运行状态、进度、阶段说明、完成结果和失败原因；
3. 数据管理页每秒轮询任务状态；
4. 页面离开后任务继续运行，重新进入可恢复进度；
5. 系统设置保存后立即返回，再启动后台重算；
6. 评论导入先提交数据事务，成功后再启动后台重算；
7. 运行中禁止重复创建同类任务。

**效果**：设置保存和评论导入不再被全量分析阻塞，已保存的数据不会因为页面超时而丢失。

---

### 七、报告中心与系统设置优化

#### 1. 报告中心

1. 增加报告搜索；
2. 增加开始日期和结束日期筛选；
3. 展示筛选后的报告数量；
4. 增加生成中状态；
5. 记录生成成功或失败历史；
6. 接口失败时支持重试。

#### 2. 系统设置

1. 使用正式弹窗替代 `prompt` 和普通删除确认；
2. 分类新增、编辑和删除集中处理；
3. 编辑分类时同时维护关键词；
4. 保存前展示分类数量、紧急关键词数量和重新分析影响；
5. 保存完成后启动后台重新分析任务。

**尚待独立升级**：当前报告仍沿用现有 PDF 和 Word 导出方案；真正的原生 `.docx` 与文字可选择型 PDF 需要单独升级文档生成引擎。

---

### 八、帖子列表与详情评论数不一致修复

**用户反馈**：舆情监测列表显示某帖子评论数为 0，但进入帖子详情后显示 4 条评论。

**核对结果**：

- 帖子 ID：`2127696592`；
- 原始帖子数据中的 `comment_count` 为 0；
- 评论数据中实际有 4 条记录通过 `thread_id=2127696592` 关联。

**根因**：

- 列表读取帖子表中的抓取声明值；
- 详情读取评论表中的实际关联数量；
- 两个页面使用了不同数据源。

**修复方案**：

1. 列表和详情统一展示实际已关联评论数；
2. 原始抓取评论数保留为独立后台字段，用于数据质量排查；
3. 列表使用一次批量聚合查询统计当前页评论数，避免逐条查询；
4. 增加“原始值 0、实际关联 4 条时列表必须显示 4”的回归测试。

**结果**：列表与详情评论数口径一致。

---

### 九、新增数据防丢失与一致性优化

**审计发现的风险**：

1. 同一批帖子中出现重复 ID 时，可能重复保存或后写覆盖；
2. 已存在帖子再次导入时，正文、浏览量、点赞量等更新会被跳过；
3. 稀疏增量数据可能误清空已有字段；
4. 重复评论 ID 在同一批次中会被当作错误；
5. 评论先到、帖子后到时容易被误认为评论丢失；
6. 非法发布时间曾被替换为当前时间，影响趋势和报告；
7. 评论导入后的同步全量重算可能导致请求超时。

**优化方案**：

1. 帖子和评论均按 ID 执行幂等增量合并；
2. 同批重复 ID 合并为一条，后出现的有效字段更新前一条；
3. 已存在帖子允许更新正文、作者、分类、发布时间、浏览量、点赞量和原始评论量；
4. 仅更新请求中明确提供的有效字段，空值不清除旧数据；
5. 人工分类、人工风险、复核说明、复核人和复核时间不被导入覆盖；
6. 评论先于帖子导入时照常保存，并计入“暂未匹配”；
7. 对应帖子以后导入后，重新分析会自动应用已有评论；
8. 非法时间保留为空并进入数据质量检查，不再伪造成当前时间；
9. 评论数据提交成功后再启动后台分析，避免超时造成重复导入；
10. 导入结果增加 `updated`、`duplicatesMerged`、`unmatched` 和后台任务信息。

**真实数据修复结果**：

1. 新版本首次启动补齐过去因“已有 ID 直接跳过”而遗漏的 1,063 条帖子字段；
2. 第二次使用同一数据源启动时：
   - 9,753 条帖子：新增 0、更新 0、全部跳过；
   - 12,779 条评论：新增 0、更新 0、全部跳过；
3. 证明当前重复导入具备幂等性；
4. 现有 722 条未匹配评论仍完整保留，待对应帖子出现后可继续关联。

---

### 十、验证结果

1. 后端自动化测试由 29 项增加至 37 项，全部通过；
2. 新增测试覆盖后台重算、事件编辑冲突、评论计数统一、同批重复帖子、稀疏更新保护、非法时间、重复评论和评论先到等场景；
3. 前端 TypeScript 检查和生产构建通过；
4. 前端开发服务运行在 `5173`；
5. 后端服务运行在 `8080`；
6. 最新后端进程 PID 为 `40132`；
7. 连续两次真实启动同步验证通过，第二次没有产生重复新增或重复更新；
8. 9,753 条帖子和 12,779 条评论均可正常保留、关联、分析和统计。
