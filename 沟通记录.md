# 项目沟通记录

## 2026-07-15

### 一、前端精细化升级

#### A. 接入真实 ECharts 图表

**问题描述**：所有图表区域均为文字占位符，视觉效果差。

**解决方法**：
1. 安装 `echarts` 依赖
2. 封装 [BaseChart.vue](frontend/src/components/BaseChart.vue) 组件，统一处理图表初始化、resize、销毁
3. 创建 [chartTheme.ts](frontend/src/utils/chartTheme.ts)，统一配色体系和图表配置
4. 替换所有页面图表占位符：
   - Dashboard：多折线趋势图（带渐变填充）+ 环形饼图
   - Trends：面积图、情绪折线图、堆叠面积图、渐变柱状图
   - EventDetail：帖子时间趋势小折线、风险雷达图

**涉及文件**：
- `frontend/src/components/BaseChart.vue`（新建）
- `frontend/src/utils/chartTheme.ts`（新建）
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/Trends.vue`
- `frontend/src/pages/EventDetail.vue`

---

#### B. 图标系统升级

**问题描述**：页面使用大量 emoji（💬🔴⚠️📈🍩🛡️等），风格不统一，不够专业。

**解决方法**：
1. 安装 `lucide-vue-next` 矢量图标库
2. 封装 [AppIcon.vue](frontend/src/components/AppIcon.vue) 组件，统一映射图标名称与 lucide 组件
3. 路由配置中 emoji 图标改为字符串名称（`📊` → `layout-dashboard`）
4. 替换所有页面的 emoji 为矢量图标：侧栏导航、Logo、顶栏、统计卡、Monitoring、EventDetail、Reports、Assistant

**涉及文件**：
- `frontend/src/components/AppIcon.vue`（新建）
- `frontend/src/router/index.ts`
- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/Monitoring.vue`
- `frontend/src/pages/EventDetail.vue`
- `frontend/src/pages/Reports.vue`
- `frontend/src/pages/Assistant.vue`

---

#### C. 统计卡增强 + 微动效

**问题描述**：统计卡只有数字和文字，缺乏动态效果和趋势展示。

**解决方法**：
1. 创建 `lastNDays()` 日期生成函数，避免假日期
2. 添加 [useCountUp.ts](frontend/src/utils/useCountUp.ts) composable，实现数字从0增长的缓动动画（easeOutCubic）
3. 统计卡底部添加迷你 sparkline 折线图，展示近7天趋势
4. 修复趋势箭头语义 bug：安全事件减少用绿色 ↓（`trend: 'good'`）
5. 高风险预警圆点添加 `pulse-dot` 脉冲扩散动画
6. 卡片 hover 添加 `-translate-y-0.5` 浮起效果

**涉及文件**：
- `frontend/src/utils/useCountUp.ts`（新建）
- `frontend/src/utils/chartTheme.ts`
- `frontend/src/style.css`
- `frontend/src/pages/Dashboard.vue`

---

#### E+F. 事件详情时间轴 + 风险雷达

**问题描述**：事件详情页相关帖子用表格展示不够直观，风险判断缺乏可视化。

**解决方法**：
1. 在风险判断依据旁添加6维度雷达图（讨论量/增长速度/负面情绪/事件类型/影响范围/紧急程度）
2. 相关帖子表格改为竖向时间轴布局：左侧竖线 + 圆点节点 + 内容卡片，更直观呈现事件演进

**涉及文件**：
- `frontend/src/pages/EventDetail.vue`

---

### 二、问题修复

#### 问题1：圆角矩形内文字遮挡

**问题描述**：卡片使用 `rounded-2xl` + `overflow-hidden` 导致内容被裁切，风险等级徽章被遮挡。

**解决方法**：
1. 将所有圆角矩形改为直角（移除 `rounded-2xl`、`rounded-xl`、`rounded-lg`）
2. 移除 `.stat-card` 的 `overflow-hidden` 属性
3. 所有卡片、徽章、按钮、输入框统一改为直角样式

**涉及文件**：
- `frontend/src/style.css`
- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/EventDetail.vue`
- `frontend/src/pages/Settings.vue`
- `frontend/src/pages/Reports.vue`
- `frontend/src/pages/Assistant.vue`
- `frontend/src/pages/Trends.vue`

---

#### 问题2：报告中心和系统设置页面适配问题

**问题描述**：Settings 页面使用 `max-w-3xl` 限制宽度，大屏下右侧大片空白；Reports 预览区按钮在窄屏挤压。

**解决方法**：
1. Settings：移除 `max-w-3xl`，改为响应式两列布局（`xl:grid-cols-2`），议题分类改为两列排列
2. Reports：预览区头部改为 `flex-wrap`，按钮组允许换行，预览文本容器加 `overflow-x-auto`

**涉及文件**：
- `frontend/src/pages/Settings.vue`
- `frontend/src/pages/Reports.vue`

---

#### 问题3：图表假日期（6/31、6/41）

**问题描述**：趋势分析页面面积图 X 轴日期使用字符串拼接 `${6}/${28 + i}`，导致6月出现不存在的31日、41日等假日期。

**解决方法**：
1. 创建 `lastNDays(n)` 函数，使用 JavaScript `Date` 对象动态生成日期标签
2. 自动处理跨月问题，确保日期有效性
3. 替换所有硬编码日期数组

**涉及文件**：
- `frontend/src/utils/chartTheme.ts`

---

#### 问题4：趋势分析图表与实际数据不符

**问题描述**：趋势分析页面四个图表显示的数值仍是 `chartTheme.ts` 中的默认 mock 数据，未使用后端真实数据；TOP5 议题显示不合理增长率。

**根本原因**：前后端字段名不匹配，前端无法读取后端数据，图表 fallback 到默认 mock 值。

**解决方法**：
1. 后端字段对齐：`days`→`labels`，`values`→`data`，堆叠图改为 `series: [{name, data}]` 格式
2. TOP5 占比基数修复：改为使用全部安全相关帖子数作为分母，反映真实占比
3. TOP5 语义明确：从"增长率"改为"占比"，标题改为"安全议题分布 TOP5"
4. 面积图时间窗口统一：从14天改为7天
5. 清除默认 mock 数据，避免误导

**涉及文件**：
- `backend/src/main/java/com/nankai/yuqing/controller/TrendsController.java`
- `frontend/src/pages/Trends.vue`
- `frontend/src/utils/chartTheme.ts`

---

#### 问题5：前端卡片内边距（侧边距）问题

**问题描述**：所有卡片内文字过于贴近左边缘（顶格），多次调整 padding 后仍未生效。

**根本原因**：多个旧前端进程占用端口，用户看到的是旧代码缓存；`.card-pad`/`.stat-card` 的 padding 可能被其他规则覆盖。

**解决方法**：
1. 强制杀掉所有旧 node 进程（共6个），干净启动前端
2. 给 `.card`、`.card-pad`、`.stat-card` 的 padding 添加 `!important` 强制生效
3. 最终值：`padding: 32px 52px !important`（上下32px，左右52px）

**涉及文件**：
- `frontend/src/style.css`
- `frontend/src/layouts/MainLayout.vue`

---

#### 问题6：趋势分析第四个图表语义错误

**问题描述**：第四个柱状图标题为"各来源渠道对比"，副标题写"校园集市/小红书/微博/B站"，但本项目仅针对校园集市；后端实际按 `categoryName`（打听求助/二手闲置/恋爱交友）统计，语义不符。

**解决方法**：
1. 后端 `buildSourceData()` 改为按 `safetyCategory`（安全类别）统计
2. 前端标题改为"各类帖子类型分布"，副标题改为"按安全类别统计"
3. 验证：API 返回正确显示各类安全帖子数量，总数 907 条与真实数据一致

**涉及文件**：
- `backend/src/main/java/com/nankai/yuqing/controller/TrendsController.java`
- `frontend/src/pages/Trends.vue`

---

#### 问题7：前后端数据校验

**问题描述**：确认每个板块的数据分析是否基于真实的 907 条爬取数据。

**校验结果**：
- ✅ Dashboard：总量 907，安全事件 23，高风险 3，与真实数据一致
- ✅ Monitoring：分页查询基于真实数据，搜索/筛选/分页全部可用
- ✅ Trends：4个图表数据均来自后端真实统计（areaData: [0,0,0,0,350,401,156]，总量 907）
- ✅ 事件管理：基于真实帖子聚合，无随机数
- ✅ 所有统计逻辑已移除 `Math.random()`，全部基于数据库真实数据

**涉及文件**：全项目前后端接口

---

### 三、后端接口数据结构梳理

**任务描述**：梳理前端所需后端 API 接口和数据结构，供后端开发参考。

**输出**：整理了8个核心 API 接口（Dashboard、事件列表、事件详情、舆情监测、趋势分析、报告中心、系统设置、智能助手）及对应的 JSON 数据结构，并给出数据库表设计建议。

---

## 2026-07-16

### 一、项目识别与新增数据核验

#### 1. 项目功能与需求梳理

**任务描述**：识别项目现有内容，理清业务目标、主要功能、技术结构与待完善需求。

**梳理结果**：
1. 项目定位为“校园集市安全舆情分析与研判平台”
2. 业务链路为：帖子导入 → 分类与情绪分析 → 风险评分 → 事件聚合 → 人工研判 → 趋势与报告
3. 前端包含舆情总览、舆情监测、事件管理、趋势分析、智能助手、报告中心、系统设置和数据管理等模块
4. 后端采用 Spring Boot + JPA + H2，前端采用 Vue 3 + TypeScript + ECharts
5. 识别出原有规则误报、事件过度聚合、设置未持久化、任务二标准未到位等问题

---

#### 2. 新增近万条数据体检

**数据文件**：`posts(2).json`

**核验结果**：
- 数据总量：9,753 条
- 原 `posts.json`：907 条
- 新文件已完整包含原 907 条数据，不能直接叠加
- 新文件内部重复 ID：0 条
- 正文缺失：671 条，但标题和发布时间完整
- 时间范围：2026-05-26 至 2026-07-15
- 来源分类：二手闲置 5,083 条、打听求助 3,606 条、恋爱交友 1,054 条、兼职招聘 10 条

**解决方法**：
1. 后端默认读取 `posts(2).json`
2. 启动时按帖子 ID 自动增量导入，已有数据不会重复写入
3. 数据库已有旧数据时不再直接跳过初始化，而是继续检查新增记录
4. 分析规则版本变化时自动识别旧结果并重新分析

---

### 二、舆情分析规则 2.0

#### 1. 分类误报修复

**原问题**：旧规则使用“买、卖、出、钱、火、打”等单字，只要命中任意一个词就分类，导致普通二手交易和日常讨论大量误报。

**优化内容**：
1. 改为带权短语、标题加权、来源排除和上下文组合
2. 普通二手商品不会因为“转账、充电、电动车、自行车”等词被判为诈骗、消防或交通事件
3. 诈骗分类需要“被骗、诈骗、收钱不发货、转账后失联”等明确证据
4. 消防、宿舍和餐饮分类增加真实场景或故障证据门槛
5. 新增分类置信度、细分话题和分析版本字段

---

#### 2. 中文歧义与网络用语修复

**抽样发现的问题**：
- “实习要不要跑路”误判为诈骗
- “考试爆炸”“游戏产能爆炸”误判为突发事件
- “救命，谁能借卫生纸”误判为突发或餐饮问题
- “入室抢劫的爱情”误判为治安事件
- “私发骚扰老师要求捞分”误判为人身骚扰
- 二手帖子中的“骗子别来”误判为诈骗事件

**解决方法**：增加真实事故上下文、餐饮场景、设施故障和普通二手交易排除规则。上述样例均已归为普通讨论。

---

#### 3. 风险评分与事件聚合升级

**优化内容**：
1. 风险评分综合类别严重程度、分类置信度、负面情绪、紧迫词、浏览量和互动量
2. 非安全帖子风险评分归零，避免高热度普通帖子变成高风险
3. 事件从“仅按安全大类聚合”改为“安全类别 + 细分话题 + 自然周”聚合
4. 使用稳定事件 ID，重复分析不会生成重复事件，并保留处置状态
5. 事件时间改为相关帖子实际发布时间，不再全部显示为系统分析时间

**最终全量验证结果（规则 2.0）**：
- 总帖子：9,753 条
- 明确安全相关：81 条
- 普通讨论：9,672 条
- 聚合事件：11 个
- 高风险帖子：1 条
- 中风险帖子：17 条
- 低风险帖子：9,735 条

---

### 三、数据与性能优化

#### 1. 万条数据性能适配

**优化内容**：
1. 帖子监测由“全部查询后在内存分页”改为数据库分页
2. 单次分页上限设为 200 条
3. 增量导入只查询已有帖子 ID，不再加载所有完整实体做去重
4. 开启 Hibernate 批量插入与批量更新
5. 重新分析和数据导入接口延长超时时间

---

#### 2. 数据管理与分析质量展示

**新增指标**：
- 安全相关帖子数
- 普通讨论数
- 安全内容占比
- 高置信分类占比
- 正文缺失数量
- 当前分析规则版本

同时修复了重新分析完成后前端统计卡被成功消息覆盖的问题。

---

### 四、任务二扩展接口

**背景**：任务二分析标准尚未收到，不能提前猜测评分逻辑。

**解决方法**：
1. 新建统一的 `AnalysisTaskExtension` 扩展契约
2. 预留任务二状态与执行接口
3. 明确可使用的帖子标准化字段
4. 当前状态为 `WAITING_FOR_STANDARD`
5. 任务二未启用时不会影响核心分析结果

**接口**：
- `GET /api/analysis/extensions/task2`
- `POST /api/analysis/extensions/task2/run`

---

### 五、系统设置持久化漏洞修复

#### 1. 分类刷新后消失

**问题描述**：前端虽然提交新增分类，但后端 `PUT /api/settings` 只返回“保存成功”，没有保存请求；刷新后 `GET /api/settings` 又返回硬编码默认分类。

**解决方法**：
1. 新增 `system_settings` 数据表
2. 风险阈值、分类和自定义关键词以 JSON 形式持久化到 H2
3. 刷新页面和重启后端后仍可读取已保存设置
4. 增加重复分类、空分类、关键词长度和阈值关系校验

---

#### 2. 设置未参与实际分析

**原问题**：风险阈值只在设置页显示，没有参与帖子和事件分级；新增分类也没有分析规则。

**解决方法**：
1. 风险阈值真正参与帖子、事件及人工研判分级
2. 新增自定义分类时必须填写至少一个不少于 2 个字符的识别关键词
3. 自定义分类关键词参与后续帖子分析
4. 删除内置分类可停用对应识别规则
5. 保存设置后自动重新分析全部帖子和事件
6. 舆情监测与事件管理的分类筛选改为动态读取系统设置
7. 修复“其他”分类无法筛选的问题

**持久化验证**：使用临时分类完成“保存 → 后端重启 → 重新读取”测试，确认设置仍存在；验证结束后已清除测试分类并恢复默认 70/40 阈值。

---

### 六、测试、启动与安全检查

#### 1. 构建与测试

- 后端自动化测试：16 项全部通过
- 前端 TypeScript 与正式构建：通过
- 全量 9,753 条数据内存库验证：通过
- 分页、筛选、设置保存、自动重分析、任务二接口：通过
- 项目启动地址：前端 `http://localhost:5173`，后端 `http://localhost:8080/api`

---

#### 2. GitHub 提交前信息安全审计

**检查范围**：待提交源码、配置、测试、文档、Git 历史和远程地址。

**检查结果**：
1. 待提交文件中未发现 API Key、访问令牌、密码、私钥或带凭证的远程地址
2. 待提交文件中未发现邮箱、手机号或身份证号等个人联系方式
3. `package-lock.json` 的依赖完整性哈希产生一次规则误报，确认不是密钥
4. Git 历史中未发现原始帖子数据或本地数据库
5. 发现旧提交作者元数据使用了真实姓名和 QQ 邮箱；因远程仓库为私密仓库并需保留原版本，已恢复原 3 个提交及其哈希，不再改写原提交身份；今天新增提交使用 GitHub 用户名及 `users.noreply.github.com` 匿名地址
6. `posts(2).json` 含用户昵称、头像地址和帖子正文，不纳入 Git
7. `backend/data/` 含本地 H2 完整数据，不纳入 Git
8. `.claude/`、环境变量文件、数据库文件和原始抓取数据已加入 `.gitignore`

**推送范围**：仅推送安全的源代码、自动化测试、项目说明、依赖清单和沟通记录。

---

## 2026-07-18

### 一、项目启动与联调环境

#### 1. 前后端启动

**沟通需求**：先启动项目，随后补充要求后端也同时启动，便于现场查看并持续提出前端调整意见。

**处理结果**：
1. 启动 Vue 前端开发服务，地址为 `http://127.0.0.1:5173`
2. 打包并启动 Spring Boot 后端，API 地址为 `http://127.0.0.1:8080/api`
3. 后端启动后完成 9,753 条帖子和 12,779 条评论的增量同步检查
4. 保持开发服务运行，使前端修改可通过热更新直接查看

---

#### 2. 登录失败与服务重启排查

**用户反馈**：新增登录后暂时无法登录，要求重新启动前后端。

**根本原因**：
1. 当时 5173 和 8080 端口均没有有效服务
2. 重新启动前端时发现 5173 被旧 Node 进程残留占用，新服务自动切换到了 5174

**解决方法**：
1. 清理占用 5173、5174 的残留前端进程
2. 将前端固定重新启动到 5173
3. 重新打包并启动后端到 8080
4. 使用真实接口完成联调验证：前端首页返回 200、未登录接口返回 401、正确登录返回 200、登录状态查询返回 200

---

### 二、全局页面排版与可读性优化

#### 1. 侧边栏字体与导航布局

**用户反馈**：侧边栏字体偏小、位置过于靠左，与整体页面观感不协调；随后进一步明确需要放大后四个目录“详细内容”的字体，而不只是目录名称。

**解决方法**：
1. 放大侧边栏导航文字和图标
2. 导航项内容改为居中排列，统一图标与文字间距
3. 放大事件管理、报告中心、系统设置、数据管理四个页面的正文、表格、徽章和辅助说明字体
4. 保留页面信息密度，同时提高大屏环境下的可读性

---

#### 2. Logo 区重新设计

**用户反馈**：原 Logo 和文字尺寸与页面整体风格不匹配。

**解决方法**：
1. 重做侧边栏顶部品牌区
2. 使用统一的盾牌矢量图标、科技感边框和状态点
3. 调整“校园安全舆情”和副标题的字号、间距与字重
4. 与深色渐变侧栏、导航图标和主页面字体体系保持一致

---

#### 3. 各页面顶部标题位置统一

**用户反馈**：舆情总览、舆情监测、事件管理等页面顶部标题过于靠左顶格，希望一次性解决所有页面。

**沟通确认**：用户特别澄清此项只修改页面顶部标题，不应联动修改其他矩形卡片。

**解决方法**：
1. 将顶部标题缩进统一收口到主布局的 `.app-header`
2. 桌面端统一设置左右内边距，移动端保留响应式间距
3. 撤回与顶部标题无关的卡片范围修改，避免影响其他矩形框
4. 后续卡片间距作为独立需求单独处理

---

#### 4. 卡片间距与页面留白统一

**用户反馈**：总览页顶部统计卡上方和左右有间距，但与下方趋势卡片之间没有间隔，风格不统一。

**解决方法**：
1. 统一总览页各区域的纵向间距
2. 使顶部统计卡与下方趋势区域之间保留与左右一致的视觉留白
3. 继续保持卡片直角设计，不重新引入圆角

---

#### 5. 字体颜色与背景对比度

**用户反馈**：部分日期、辅助文字和帖子内容颜色过浅，与背景对比不明显。

**解决方法**：
1. 加深日期和辅助信息颜色
2. 统一浅色背景上的次级文字对比度
3. 为深色侧栏单独保留更明亮的辅助文字颜色，避免全局颜色规则影响侧栏
4. 放大舆情监测中的帖子标题、正文和元信息字体

---

#### 6. 搜索框与选择框

**用户反馈**：搜索框、筛选选择框上下过窄，文字较小，界面显得拥挤。

**解决方法**：
1. 统一输入框和选择框的最小高度
2. 增加上下内边距
3. 放大输入文字和筛选文字
4. 保持搜索框、类型、情绪、分类筛选控件的高度一致

---

### 三、舆情总览与趋势分析合并

#### 1. 功能合并与目录去重

**用户反馈**：舆情总览与趋势分析功能存在重合，项目负责人要求合并去重并放在同一目录。

**解决方法**：
1. 将趋势研判内容并入舆情总览页面
2. 侧边栏不再单独显示“趋势分析”
3. 保留旧 `/trends` 地址并重定向到 `/dashboard`，避免旧链接失效
4. 趋势图继续使用后端真实数据，不重复保留同类统计模块

---

#### 2. 趋势研判标题和时间范围

**用户反馈**：趋势研判标题位置需要调整；7天、30天、自定义时间范围点击无效。

**解决方法**：
1. 调整趋势研判标题和时间筛选区的内边距及对齐
2. 补齐 7 天、30 天和自定义时间范围的交互状态
3. 时间范围变化后重新请求对应数据并更新图表
4. 保留刷新入口，用于主动重新加载当前范围

---

### 四、帖子与评论后端关联

#### 1. 评论数据导入与关联

**用户需求**：修改后端，将帖子和评论连接起来，点击帖子后能够查看该帖评论。

**解决方法**：
1. 新增评论实体、数据仓库、导入服务和启动初始化器
2. 默认读取项目根目录的 `comments.json`
3. 评论以 `comment_id` 增量同步，通过 `thread_id` 与帖子 `id` 关联
4. 未匹配评论继续保留，但不参与帖子风险评判
5. 数据库不保存评论者昵称、头像及用户标识，仅保留正文和分析必要字段

**数据验证**：
- 评论总数：12,779 条
- 已关联评论：12,057 条
- 未匹配评论：722 条
- 已覆盖帖子：1,755 条

---

#### 2. 帖子详情与评论查看

**解决方法**：
1. 新增帖子详情路由和 `PostDetail.vue`
2. 舆情监测列表中的帖子支持点击进入详情
3. 新增按帖子 ID 分页读取评论的接口
4. 详情页展示原帖信息、分析结果、评论列表和评论风险依据
5. 返回列表时保留清晰的页面层级

**新增接口**：
- `GET /api/comments/post/{postId}`

---

#### 3. 评论辅助评判规则 2.1.3

**处理原则**：
1. 评论不会直接覆盖原帖的安全分类
2. 多条同类风险评论可形成“建议人工复核”提示
3. 已分类帖子可根据评论中的同类证据、负面比例、互动量和紧急表达获得有限风险加权
4. 评论结果单独展示，便于管理人员核对分析来源

---

### 五、系统设置与任务二接口调整

#### 1. 删除手动风险阈值

**用户需求**：系统设置中不再手动量化高、中、低风险分数；任务二同学只传递低风险、中风险、高风险标记。

**解决方法**：
1. 删除系统设置页的风险阈值调节界面
2. 后端设置接口不再依赖人工配置的分数阈值
3. 任务二扩展契约改为只接收离散风险等级
4. 风险等级限定为低、中、高，不传递分数或阈值
5. 任务二结果作为独立扩展，不影响现有核心分析流程

**任务二数据约定**：

```json
{
  "results": [
    { "postId": "帖子ID", "riskLevel": "低" }
  ]
}
```

---

### 六、报告中心功能修复

#### 1. 日报与周报切换

**用户反馈**：报告中心只能看到日报，日报、周报无法调整。

**解决方法**：
1. 补齐日报、周报类型切换控件
2. 前端选择报告类型后携带对应参数请求后端
3. 后端根据 `daily`、`weekly` 分别计算报告周期
4. 报告列表、详情预览和导出内容同步使用当前报告类型
5. 保留事件专题报告类型的后端支持

---

### 七、智能助手与侧边栏交互

#### 1. 智能助手改为悬浮模式

**用户需求**：不再将智能助手作为普通侧边栏目录，改成悬浮入口。

**解决方法**：
1. 从侧边栏隐藏独立的智能助手菜单项
2. 在页面右下角增加全局悬浮助手按钮
3. 保留在线状态提示和展开交互
4. 旧 `/assistant` 地址重定向到总览，避免出现重复入口

---

#### 2. 大模型风格的侧边栏收起效果

**用户需求**：参考大模型聊天界面的侧栏收起方式。

**解决方法**：
1. 删除侧边栏底部的“收起”文字按钮
2. 在页面顶部左侧增加面板切换图标
3. 点击后整个侧边栏平滑滑出，主内容区同步扩展
4. 收起后保留展开按钮，可随时恢复侧边栏
5. 增加悬停、按压和宽度变化动画
6. 移动端继续使用原有抽屉式菜单逻辑

---

### 八、最简昵称与密码登录

#### 1. 后端登录与会话

**用户需求**：更新前后端设置，先实现最简单的昵称和密码登录。

**解决方法**：
1. 新增登录、当前用户和退出登录接口
2. 登录成功后生成随机会话令牌，默认有效期 24 小时
3. 新增后端认证拦截器，保护 `/api/**` 接口
4. 未携带有效 Bearer Token 的请求统一返回 401
5. 当前本地演示账号为“管理员”，密码为 `123456`

**新增接口**：
- `POST /api/auth/login`
- `GET /api/auth/me`
- `POST /api/auth/logout`

> 当前账号和密码为本地演示配置，正式部署前应替换，并改用加密密码存储和持久化用户表。

---

#### 2. 前端登录状态

**解决方法**：
1. 新增独立登录页面
2. 登录成功后保存令牌和昵称
3. 所有 API 请求自动携带 Bearer Token
4. 增加路由守卫，未登录时自动进入登录页
5. 接口返回 401 时清理失效状态并重新登录
6. 顶栏展示当前昵称，并增加退出登录按钮

---

#### 3. 添加登录后侧边栏目录消失

**用户反馈**：登录后原来的页面仍在，但侧边栏多个目录全部消失。

**根本原因**：新增登录页后路由顺序发生变化，侧边栏仍固定读取路由数组第一项，实际读到没有子路由的登录页。

**解决方法**：
1. 不再依赖路由数组顺序
2. 按主布局路径 `/` 精确查找导航子路由
3. 恢复舆情总览、舆情监测、事件管理、报告中心、系统设置和数据管理目录
4. 趋势分析继续按需求合并到总览，智能助手继续保持悬浮模式

---

### 九、当日验证结果

- 前端 TypeScript 检查与正式构建：通过
- 后端 Maven 自动化测试：20 项全部通过
- 后端可执行包构建：通过
- 前端 5173 服务：启动并返回 200
- 后端 8080 服务：启动成功
- 未登录访问受保护接口：返回 401
- 正确昵称和密码登录：返回 200
- 登录状态查询：返回 200，并正确识别“管理员”
- 会话令牌长度：32 位
- 侧边栏目录修复：开发服务热更新生效
- 大模型风格侧栏动画：前端构建通过并热更新生效

---

### 十、当日主要涉及文件

**前端**：
- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/router/index.ts`
- `frontend/src/pages/Login.vue`
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/Monitoring.vue`
- `frontend/src/pages/PostDetail.vue`
- `frontend/src/pages/Reports.vue`
- `frontend/src/pages/Settings.vue`
- `frontend/src/pages/EventList.vue`
- `frontend/src/pages/DataManage.vue`
- `frontend/src/components/AppIcon.vue`
- `frontend/src/components/FloatingAssistant.vue`
- `frontend/src/style.css`
- `frontend/src/utils/api.ts`

**后端**：
- `backend/src/main/java/com/nankai/yuqing/controller/AuthController.java`
- `backend/src/main/java/com/nankai/yuqing/service/AuthService.java`
- `backend/src/main/java/com/nankai/yuqing/config/AuthInterceptor.java`
- `backend/src/main/java/com/nankai/yuqing/config/CorsConfig.java`
- `backend/src/main/java/com/nankai/yuqing/controller/CommentController.java`
- `backend/src/main/java/com/nankai/yuqing/service/CommentImportService.java`
- `backend/src/main/java/com/nankai/yuqing/config/CommentDataInitializer.java`
- `backend/src/main/java/com/nankai/yuqing/controller/ReportController.java`
- `backend/src/main/java/com/nankai/yuqing/controller/SettingsController.java`
- `backend/src/main/java/com/nankai/yuqing/service/Task2AnalysisExtension.java`
- `backend/src/main/resources/application.yml`
